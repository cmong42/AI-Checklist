[phases.setup]
nixPkgs = ["cmake", "gcc", "gcc-unwrapped"]

[phases.install]
cmds = ["...", "CMAKE_ARGS=-DLLAMA_NO_OPENMP=1 FORCE_CMAKE=1 pip install --upgrade --force-reinstall --no-cache-dir --no-binary llama-cpp-python"]
